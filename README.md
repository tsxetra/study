# Study OS workspace

The React application lives in `study-app/`. Install dependencies and start the dev server with:

```bash
cd study-app
npm install
npm run dev
```

See `study-app/README.md` for feature highlights, architecture notes, and testing instructions.
